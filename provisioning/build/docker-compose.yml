version: "3.7"

services:
  ang-mat:
    container_name: ang-mat
    image: ang-material:v0.1.0
    ports:
      - 8080:80
    # volumes: 
    #   - ./data/app:/app
    #   - ./data/app/modules:/app/node_modules